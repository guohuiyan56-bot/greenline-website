# 物流追踪后台编辑说明（GREENLINE）

客户在官网 `track.html` 输入**单号**，页面会显示**由后台人员编写的那段信息**。
这些信息存放在网站根目录的 `track-data.json` 文件里，后台人员负责维护它。

---

## 一、数据文件结构（track-data.json）

```json
{
  "entries": [
    {
      "code": "GL2026-001",
      "info": "您的货物已于...装船，预计...抵达...。\n可换行写多行。",
      "updated": "2026-07-27"
    }
  ]
}
```

- `code`：客户在前台输入的**单号**（区分大小写不敏感，前后空格会自动忽略）。
- `info`：后台编写的**信息正文**，`\n` 表示换行。
- `updated`：信息更新日期（可选，会显示在结果下方）。

---

## 二、后台人员怎么写新信息（两种方式）

### 方式 A：用编辑页（推荐，无需懂代码）
1. 浏览器打开 `admin.html`（网站根目录）。
2. 填「单号」+「信息」+「更新日期」→ 点「添加到列表」。
3. 可继续添加多条；如需修改已有，先「导入现有 JSON」再编辑。
4. 点「导出 track-data.json」下载文件。
5. 把下载的文件发给技术同事，替换网站里的 `track-data.json` 并推送；
   或你自己有 GitHub 权限，直接到仓库编辑/上传该文件并提交。

### 方式 B：直接改文件（有 GitHub 权限）
- 打开仓库 `track-data.json`，按上面的结构照抄一条新记录，提交即可。
- 1 分钟内全站生效。

---

## 三、客户怎么查
- 官网导航点「🚚 物流追踪」→ 输入单号 → 点查询 → 立即看到后台编写的信息。
- 查不到会提示核对单号或联系客服，不会泄露其他单号的数据。

---

## 四、注意事项
- 这是**纯静态站**，`track-data.json` 是公开文件（懂技术的人能下载全部单号+信息）。
  对普通外贸场景足够安全（客户只知道自己的单号）。若单号属敏感信息，请另行评估。
- 每台电脑的 `admin.html` 列表存在本地浏览器，导出后才算真正发布；换电脑需重新导入。
- 不要删除 `code` 字段为空或重复的单号，避免客户查不到。
